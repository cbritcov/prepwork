<?php

class view_helper_pagetitle
{
	public function __construct($options)
	{
	    if(empty($options['title']))
	        throw new exception("XHTML requires title to be set in page title");
		echo "\n<title>{$options['title']}</title>\n";
	}
}